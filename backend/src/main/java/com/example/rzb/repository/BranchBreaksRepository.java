package com.example.rzb.repository;

import com.example.rzb.entity.BranchBreaks;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BranchBreaksRepository extends JpaRepository<BranchBreaks, Long> {
}
